# drawing-in-python
Image generation using the library Pillow (Python)

## Using

1. Create virtual environment

```sh
python3 -m venv venv
```

2. Install dependencies (Pillow)

```sh
pip install -r requirements.txt
```

3. Generate image

```sh
python draw.py
```
